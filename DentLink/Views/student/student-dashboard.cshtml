@model DentLink.BusinessLogicLayer.DTOs.Doctordtos.DoctorDashboardDto
@{
    Layout = null;
    var doctorName = ViewBag.DoctorName as string ?? "Student";
    var firstName = doctorName.Split(' ')[0];
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - DentLink</title>
    <link rel="stylesheet" href="~/assets/css/student-dashboard.css">
    <link rel="stylesheet" href="~/assets/css/responsive.css">
</head>
<body>
    <div class="student-dashboard">
        <aside class="student-sidebar">
            <div class="student-sidebar-header">
                <a href="@Url.Action("Dashboard", "Doctor")" class="student-logo">Dent<span>Link</span></a>
                <span class="student-role-badge">DENTAL STUDENT</span>
            </div>

            <nav class="student-nav">
                <a href="@Url.Action("Dashboard", "Doctor")" class="active">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7" rx="1" /><rect x="14" y="3" width="7" height="7" rx="1" /><rect x="3" y="14" width="7" height="7" rx="1" /><rect x="14" y="14" width="7" height="7" rx="1" /></svg>
                    Dashboard
                </a>
                <a href="@Url.Action("AvailableCases", "Doctor")">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 12h-4l-3 9L9 3l-3 9H2" /></svg>
                    Available Cases
                </a>
                <a href="@Url.Action("MyRequests", "Doctor")">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z" /><polyline points="14 2 14 8 20 8" /><line x1="16" y1="13" x2="8" y2="13" /><line x1="16" y1="17" x2="8" y2="17" /></svg>
                    My Requests
                </a>
                <a href="@Url.Action("Sessions", "Doctor")">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" /><line x1="16" y1="2" x2="16" y2="6" /><line x1="8" y1="2" x2="8" y2="6" /><line x1="3" y1="10" x2="21" y2="10" /></svg>
                    Sessions
                </a>
                <a href="@Url.Action("Profile", "Doctor")">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2" /><circle cx="12" cy="7" r="4" /></svg>
                    Profile
                </a>
            </nav>

            <div class="student-sidebar-footer">
                <div class="student-user-card">
                    <img src="https://i.pravatar.cc/72?img=12" alt="@doctorName" class="student-user-avatar">
                    <div class="student-user-info">
                        <div class="student-user-name">@doctorName</div>
                        <div class="student-user-role">Dental Student</div>
                    </div>
                </div>
                <form asp-controller="Account" asp-action="Logout" method="post">
                    <button type="submit" class="student-logout" style="background:none;border:none;width:100%;cursor:pointer;">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4" /><polyline points="16 17 21 12 16 7" /><line x1="21" y1="12" x2="9" y2="12" /></svg>
                        Logout
                    </button>
                </form>
            </div>
        </aside>

        <div class="student-main">
            <header class="student-topbar">
                <span class="student-topbar-title">Dashboard</span>
            </header>

            <div class="student-content">
                <div class="student-welcome">
                    <h1>Welcome back, @firstName</h1>
                    <p>Here is your clinical activity overview.</p>
                </div>

                <div class="student-stats">
                    <div class="student-stat-card">
                        <div class="student-stat-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="7" width="20" height="14" rx="2" /><path d="M16 7V5a2 2 0 00-2-2h-4a2 2 0 00-2 2v2" /></svg>
                        </div>
                        <div>
                            <div class="student-stat-value">@Model.AvailableCasesCount</div>
                            <div class="student-stat-label">Available Cases</div>
                        </div>
                    </div>

                    <div class="student-stat-card">
                        <div class="student-stat-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 11.08V12a10 10 0 11-5.93-9.14" /><polyline points="22 4 12 14.01 9 11.01" /></svg>
                        </div>
                        <div>
                            <div class="student-stat-value">@Model.AcceptedRequestsCount</div>
                            <div class="student-stat-label">Accepted Requests</div>
                            <div class="student-stat-sub">@Model.UpcomingSessionsCount session(s) upcoming</div>
                        </div>
                    </div>

                    <div class="student-stat-card">
                        <div class="student-stat-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" /><line x1="16" y1="2" x2="16" y2="6" /><line x1="8" y1="2" x2="8" y2="6" /><line x1="3" y1="10" x2="21" y2="10" /></svg>
                        </div>
                        <div>
                            <div class="student-stat-value">@Model.CompletedSessionsCount</div>
                            <div class="student-stat-label">Completed Sessions</div>
                            <div class="student-stat-sub">
                                @(Model.LastCompletedSessionDate.HasValue ? $"Last: {Model.LastCompletedSessionDate:MMM d, yyyy}" : "No sessions yet")
                            </div>
                        </div>
                    </div>
                </div>

                <div class="student-grid">
                    <div class="student-panel">
                        <h2 class="student-panel-title">Recent Activity</h2>
                        @if (Model.RecentActivity != null && Model.RecentActivity.Any())
                        {
                            <ul class="student-activity-list">
                                @foreach (var a in Model.RecentActivity)
                                {
                                    <li class="student-activity-item">
                                        <span class="student-activity-dot blue"></span>
                                        <span class="student-activity-text">@a.Message</span>
                                        <span class="student-activity-time">@a.Timestamp.ToString("MMM d")</span>
                                    </li>
                                }
                            </ul>
                        }
                        else
                        {
                            <p style="font-size:12px;color:#94a3b8;">No recent activity yet.</p>
                        }
                    </div>

                    <div class="student-panel">
                        <h2 class="student-panel-title">Upcoming Sessions</h2>
                        @if (Model.UpcomingSessions != null && Model.UpcomingSessions.Any())
                        {
                            foreach (var s in Model.UpcomingSessions)
                            {
                                string badgeClass = s.Status == "in-progress" ? "student-badge-progress" : "student-badge-confirmed";
                                <div class="student-session-card">
                                    <div class="student-session-icon">
                                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" /><line x1="16" y1="2" x2="16" y2="6" /><line x1="8" y1="2" x2="8" y2="6" /><line x1="3" y1="10" x2="21" y2="10" /></svg>
                                    </div>
                                    <div class="student-session-info">
                                        <div class="student-session-name">@s.PatientName — @s.CaseType</div>
                                        <div class="student-session-date">@s.SessionStart.ToString("MMM d, yyyy, h:mm tt")</div>
                                    </div>
                                    <span class="student-badge @badgeClass">@s.Status</span>
                                </div>
                            }
                        }
                        else
                        {
                            <p style="font-size:12px;color:#94a3b8;">No upcoming sessions.</p>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>